@model ProDat.Web2.Models.RegulatoryBody

@{
    ViewData["Title"] = "Edit";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Edit</h1>

<h4>RegulatoryBody</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="RegulatoryBodyId" />
            <div class="form-group">
                <label asp-for="RegulatoryBodyName" class="control-label"></label>
                <input asp-for="RegulatoryBodyName" class="form-control" />
                <span asp-validation-for="RegulatoryBodyName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="RegulatoryBodyDesc" class="control-label"></label>
                <input asp-for="RegulatoryBodyDesc" class="form-control" />
                <span asp-validation-for="RegulatoryBodyDesc" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
